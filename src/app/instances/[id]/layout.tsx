import { InstanceWorkspace } from "@/components/instance-workspace";

export default function InstanceLayout({ children }: { children: React.ReactNode }) {
  return <InstanceWorkspace>{children}</InstanceWorkspace>;
}
